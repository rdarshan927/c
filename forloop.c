#include <stdio.h>

int main() {
  int n;
  scanf("%d", &n);
  for (int i = 0 ; i < n ; i = i + 1) {
    printf("C \n");
  }
  return 0;
}

/*    for (int i = 0; i < 5; i++)
      printf("%d \n", i);
    */

    /*#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    for (int i = 1; i <= 10; i = i+1) {
        printf("%d x %d = %d \n", n, i, i*n);
    }
  return 0;
}
*/
